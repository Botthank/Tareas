/*let numero = parseInt(prompt("Ingresa un número múltiplo de 5 para la cuenta regresiva:"));

if (numero % 5 !== 0) {
  console.log("El número ingresado no es múltiplo de 5. Inténtalo de nuevo.");
} else {
  let contador = numero;

  do {
    console.log(contador);
    contador -= 5;
  } while (contador >= 5);

  console.log("¡Despegue!");
}